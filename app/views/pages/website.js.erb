var area = document.getElementById("<%= j @name %>");

area.insertAdjacentHTML("beforeend", "<%= j render 'show_website', url: @url %>");
area.classList.remove("loading-wait");
area.querySelector(".loading").remove();
